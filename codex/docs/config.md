# 配置系统

## 配置文件

Codex 使用 `config.toml` 进行配置。

### 项目级配置

`.codex/config.toml` — 位于项目根目录，可提交到 Git。

### 全局配置

`~/.codex/config.toml` — 用户级全局配置。

## 配置内容

- 沙箱设置
- MCP 服务器
- Hooks
- 模型选择
- 推理参数

详见: https://developers.openai.com/codex/config-reference
